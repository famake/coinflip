# Environment variables for docker-compose
# Copy this file to .env and update with your values

# Your domain name (without http/https)
DOMAIN=coinflip.example.com

# Subdomains (comma-separated, or 'wildcard' for all subdomains)
# Leave as 'www' if you only want www subdomain
SUBDOMAINS=www

# Email for Let's Encrypt certificate notifications
EMAIL=your-email@example.com

# Uncomment to test Let's Encrypt setup (avoids rate limits)
# STAGING=true
